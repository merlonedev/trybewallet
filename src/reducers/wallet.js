const INICIAL_STATE = {
  currencies: [],
  expenses: [],
};

const wallets = (state = INICIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default wallets;
